<h1>All Posts</h1>
<%= link_to "New Post", new_admin_post_path %>

<ul>
  <% @posts.each do |post| %>
    <li>
      <h3><%= link_to post.title, admin_post_path(post) %></h3>
      <p><%= truncate(post.content, length: 100) %></p>
    </li>
  <% end %>
</ul>
